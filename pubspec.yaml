name: mistral_client
description: A Dart client library for the Mistral AI API.
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.2.2
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
